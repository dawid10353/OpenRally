/**
 * Centralized assets configuration.
 * Contains paths to all 3D models (GLB/GLTF), textures, and other static assets.
 */

// ─── Vehicle Models ─────────────────────────────────────────────────
/** Path to the classic rally hatchback 3D model */
export const VEHICLE_MODEL_PATH = '/models/vehicles/car.glb';

/** Path to the modern WRC rally car 3D model */
export const VEHICLE_WRC_MODEL_PATH = '/models/vehicles/rally_wrc.glb';

// Add more models here (e.g. wheels, props) as the game expands
